let input_handler = |
  #enabled: &[bool, null] = &null,
  #handle: &fn(e: Event) -> [`Stop, `Continue] throws 'e,
  child: &Tui
| -> Tui throws 'e `InputHandler({ child, enabled, handle });

let on_press = |e: Event, f: fn(k: KeyEvent) -> [`Stop, `Continue] throws 'e|
  -> [`Stop, `Continue] throws 'e select e {
    `Key(k) => select k.kind {
      `Press => f(k),
      _ => `Continue
    },
    _ => `Continue
  }
