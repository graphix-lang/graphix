type Svg = {
  source: &string,
  width: &Length,
  height: &Length,
  content_fit: &ContentFit
};

val svg: fn(
  ?#width: &Length,
  ?#height: &Length,
  ?#content_fit: &ContentFit,
  &string
) -> Widget
