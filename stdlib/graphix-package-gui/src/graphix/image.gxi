use super::{ContentFit, ImageSource, Length, Widget};
type Image = {
  source: &ImageSource,
  width: &Length,
  height: &Length,
  content_fit: &ContentFit
};

val image: fn(
  ?#width: &Length,
  ?#height: &Length,
  ?#content_fit: &ContentFit,
  source: &ImageSource
) -> Widget
