type Button = {
  child: &Widget,
  on_press: &fn(null) -> Any,
  width: &Length,
  height: &Length,
  padding: &Padding
};

val button: fn(
  ?#on_press: fn(null) -> Any,
  ?#width: &Length,
  ?#height: &Length,
  ?#padding: &Padding,
  &Widget
) -> Widget
