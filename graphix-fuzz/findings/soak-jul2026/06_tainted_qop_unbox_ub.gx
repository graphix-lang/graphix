// bisect: JIT UB — tainted qop inner unboxed Value::Null as Array
// (graphix_value_into_array_borrowed unreachable_unchecked; SIGABRT
// under debug_assertions, true UB in release). The let-in-element makes
// the literal's element Bottom-typed → tainted; the composite-success
// qop arm gated only on is_err. FIXED 2026-07-04: taint folds into the
// abort branch; unboxers hardened to defined panics.
{let a = [i64:1, let n = i64:5, i64:3]; let x = a[i64:1..]; x$}
