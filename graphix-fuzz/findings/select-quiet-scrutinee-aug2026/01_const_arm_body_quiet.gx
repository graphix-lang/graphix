// The over-fire direction of 00 (the aug05i lesson — the reverted
// 6fcd6c1e select-refire fix over-fired exactly here): a quiet
// scrutinee with a standing selection whose arm body is a CONSTANT
// must stay QUIET — no selection change, no body production, no
// emission in either mode. Pins that mem-dispatch's quiet re-entry
// produces no becoming-selected fire (the record no-ops, woke = 0)
// and the const's STALE gate holds.
// schedule-v1: cap=96 events=768; in0=i64:0; in0=i64:7
{
	let v0: Array<i64> = [];
	v0 <- in0 ~ array::push(v0, i64:0);
	let v3: i64 = {
		let v2: u8 = array::fold(v0, u8:0, |a, x| (a / a));
		select v2 { _ => i64:9 }
	};
	v3
}
