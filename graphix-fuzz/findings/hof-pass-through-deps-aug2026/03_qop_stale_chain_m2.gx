// The aug05k stale-chain member 2 (was pending-triage): interp held
// a constant 0 while jit varied — list::find + nested selects + ~ +
// try/? + checked +?. Both modes now: [0:0]; [0:-1].
// schedule-v1: cap=112 events=896; in0=i64:1
{let v1: i64 = {let v0: [i64, null] = list::find(list::from_array([in0, i64:0, i64:0]), |v0| true); select in0 {v1 if (f64:-1. > f64:-1.) => i64:100, i64:0 => in0, v1 => (select v0 {null as _ => v1, i64 as n => n} - str::len(""))}}; let v2 = in0 ~ ({ catch(e) (in0 % v1); ((v1 +? in0))? }); ((v2 - v1) - v2)}
