// PROMOTED 2026-07-18 after trace-level verification against the
// body-driven firing ruling (Eric ruled A, 2026-07-11; class closed
// 2026-07-18 — the residual DynCall always-fired arg stash was the
// same always-FIRED-delivery family as the tail-stash/arm-bind seams
// the honest-tags fix retired). Expected trace (both modes): a single 7 — firing source, body consumes nothing: quiet after init.
{let n = i64:0; select n {v if v < i64:3 => n <- (v ~ n) + i64:1, _ => never()}; let a = [n, n]; array::fold(a, i64:0, |acc, x| {array::fold([i64:5, i64:7], i64:1, |a2, x2| x2); array::fold([i64:5, i64:7], i64:0, |a3, x3| x3)})}
