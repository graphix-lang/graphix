// The strict ruling's EMPTY-FOLD leg (see 00): a bottomed INIT
// delivery poisons the empty-source fold THIS cycle. The interp's
// empty-source arm read the RETAINED self.init (last good) and rode
// it out FIRED where the kernel poisoned — the one interp-side hole
// the strict flip exposed (collection.rs, fixed with a bottom check
// before the retained-init read; the retained init still serves
// genuinely quiet cycles on the value channel).
// schedule-v1: cap=176 events=1408; in0=i64:1; in0=i64:7 in1=i64:12; in0=i64:1 in1=i64:12; in0=i64:5 in1=i64:-3; in0=i64:2; in0=i64:3 in1=i64:1; in0=i64:12 in1=i64:3
{ let v0 = select (in0 % i64:2) { i64:0 => array::len(array::window(#n: i64:3, [in0], i64:100 / (in0 % i64:3))), _ => once(in0) }; let v1: i64 = array::fold({ let mt: Array<bool> = []; mt }, (in0 / v0), |v0, v1| (i64:0 % i64:-1)); let v2 = ""; v2 <- in1 ~ "[v2]x[in1]"; let v3 = str::len(v2); ((v3 * v3) * v1) }
