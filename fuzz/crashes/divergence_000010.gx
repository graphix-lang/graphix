// bisect: GIR/emit bug (interp != fused)
// interp: Value(Bool(true))
// fused:  Timeout
// jit:    Value(Bool(true))
// mutant: { let v0 = select ((i64:7 % i64:0) + i64:2) { 42 => (i64:100 + (i64:1 - i64:1)), _ => (i64:-100 * (i64:-1 + i64:42)) }; let v1 = "a"; let v2 = select i64:-100 { 42 => (f64:0.1 + f64:2.5), _ => ((f64:-1.0 + f64:-1.0) / (f64:0.0 % f64:0.0)) }; true }
// minimized:
{null; "a"; let v2 = select i64:-100 {i64:42 => (f64:0.1 + f64:2.5), _ => ((f64:-1. + f64:-1.) / (f64:0. % f64:0.))}; true}
