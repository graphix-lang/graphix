// CRASH: child HANG (outer deadline)
// do not promote to findings/ until fixed (regress runs in-process)
// mutant:
{let rec count = |n: i64, acc: i64| -> i64 select n {i64:0 => acc, _ => count(n - i64:1, acc + i64:1)}; count(i64:-1, i64:0)}
