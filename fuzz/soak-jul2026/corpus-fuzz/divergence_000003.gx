// bisect: fusion/JIT bug (interp != jit)
// interp: Trace(Trace { epochs: [Epoch { events: [(0, F64(2.0))], capped: false }] })
// jit:    RuntimeErr("trace_wait_idle: runtime did not respond")
// mutant: {let f = 'a: Number|x: 'a| -> 'a i64:1; {let a = f(i64:3); let b = f(f64:2.5); cast<f64>(a)$ + b}}
// minimized:
{let f = 'a: Number|x: 'a| -> 'a f64:0.; {let a = f(i64:3); let b = f(f64:2.5); cast<f64>(a)$ + b}}
