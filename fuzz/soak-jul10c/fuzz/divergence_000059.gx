// bisect: fusion/JIT bug (interp != jit)
// interp: Trace(Trace { epochs: [Epoch { events: [(0, I64(0))], capped: false }] })
// jit:    Trace(Trace { epochs: [Epoch { events: [], capped: false }] })
// mutant: sync { let mut a = i64:1; let mut b = i64:1; for v in [i64:1, i64:2, i64:2] {a = a + v; b = b * v}; v % i64:2 }
// minimized:
sync { let mut a = i64:0; let mut b = i64:0; for v in [i64:0, i64:2, i64:2] {a = a + v; b = b * v}; v % i64:2 }
