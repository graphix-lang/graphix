Post class-1 fix (constrain_known/settle/BuiltInLambda, 2026-07-11):
all 36 STILL_DIVERGING re-run; 0 resolved (the fix's class — nested-map
elaboration hang — reproduced as the 10 red workspace TESTS, not as
these artifacts). Classification by divergence shape:

REPLAY LEAK (class 2, awaiting reset_replay — task #38), 19 + 1:
  fuzz/divergence_0000{05,06,09,10,11,12,13,14,15,17,21,24,33,36,38,39,41,42}.gx
  generate/divergence_0000{64,89}.gx
  Shape: interp emits stale/partial composites (errored elements kept),
  jit correctly bottoms (Trace([])). d34 is the INVERSE (interp empty,
  jit emits) — same family, verify against reset_replay when it lands.

DOUBLE-EMISSION (runtime-bound loop pacing), 14:
  generate/divergence_0000{13,14,22,28,41,45,46,55,63,67,69,79,85}.gx
  reactive/divergence_000001.gx
  Shape: interp emits the SAME value twice (events 0 and 1), jit once.
  These programs use dynamic sandbox modules, so their fold/loop call
  sites legitimately never statically resolve — the runtime
  instantiation path over-fires by one cycle in the interp (the known
  init-cycle alignment asymmetry, cf. hof_sleeping_arm's 4|5
  tolerance). Candidate outcomes: fixed by reset_replay's For rework,
  or ruled a pacing-whitelist case.

generate/divergence_000034.gx: check produced no divergence banner on
re-run (timeout or env-sensitive) — re-triage during jul10d.
