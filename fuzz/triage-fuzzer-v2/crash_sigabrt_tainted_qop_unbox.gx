// CRASH: SIGABRT — non-unwinding panic (debug UB check) in
// graphix_value_into_array_borrowed: unreachable_unchecked reached.
// A TAINTED qop inner carries the Value::Null placeholder; the clean
// disc isn't Error so the composite-success arm unboxed Null as an
// Array — true UB in release. FIXED 2026-07-04: emit_qop_node's
// String/composite arm folds is_tainted into the abort branch (deliver
// stays gated on real errors, owned inners still drop); the unboxers'
// unreachable_unchecked hardened to defined panics.
{let a = [i64:1, let n = i64:5, i64:3]; let x = a[i64:1..]; x$}
