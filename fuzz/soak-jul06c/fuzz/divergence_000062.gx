// bisect: fusion/JIT bug (interp != jit)
// interp: Timeout
// jit:    Trace(Trace { epochs: [Epoch { events: [], capped: false }] })
// mutant: {let rec fib = |n: i64| -> i64 select n {i64:0 => i64:0, i64:1 => i64:1, _ => fib(n - i64:1) + fib({let v = i64:1 / i64:-100; v; i64:42})}; fib(i64:12)}
// minimized:
{let rec fib = |n: i64| -> i64 fib(n) + fib({let v = i64:1 / i64:-100; v; i64:42}); fib(i64:12)}
